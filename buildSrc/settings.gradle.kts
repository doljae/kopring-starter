pluginManagement {
    repositories {
        gradlePluginPortal()
    }
    plugins {
        id("de.fayard.refreshVersions") version "0.60.3"
////                                # available:"0.60.3"
    }
}

plugins {
    id("de.fayard.refreshVersions")
}
